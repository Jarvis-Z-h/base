<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voidstar.base.mapper.UserDao">
 <!--<select id="selectUserById"
		resultType="com.hengsu.entity.User" parameterType="String">
		select * from user where id=#{id}
	</select>-->
	
<!--	<insert id="insertUser" parameterType="com.hengsu.entity.User">
	insert into user(id,name,sex,role,telephone) values(#{id},#{name},#{sex},#{role},#{telephone})
	</insert>-->
	
	
</mapper>













